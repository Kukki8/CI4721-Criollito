lucas x;
sencillo y;
calidad flag;
calidad velda;
beta ch;

velda = chevere;
x = 42;
y = 3.14;
flag = chevere;
ch = 'z';

x = x + 10;
x = x - 2;
x = x * 3;
x = x / 2;
x = x % 5;
x = -x;
y = -y;
x = (x + 5) * (x - 3);

lucas sumar(lucas x, lucas y) {
    lucas z = x + y;
    fuimonos z;
}

calidad eVelda(calidad x) {
  fuimonos x;
}

x = sumar(x, 15);
digalo();
rotalo("50");

porsia (flag separeceigualito velda) {
    x = x + 1;
} conchale {
    x = x - 1;
}

mientras (flag separeceigualito velda) echale pichon {
    x = x + 2;
    porsia (x mayolke 100) {
        flag = chimbo;
    }
}

x = sumar(x, y);
x = flag;
ch = x;
x = x + ch;
y = eVelda(flag);
flag = eVelda(ch);

lucas chamo;
calidad verdaita;
beta letra;
sencillo numerito;

chamo = verdaita;
verdaita = chamo;
letra = numerito;
numerito = verdaita;

chamo = letra + chamo;
verdaita = letra * verdaita;
numerito = verdaita / chamo;

lucas resultado;
resultado = sumar(verdaita, numerito);
calidad estado;
estado = eVelda(chamo);
estado = sumar(chamo, letra);
verdaita = rotalo(chamo);
letra = digalo();

porsia (numerito nosepareceigualito flag) {
    chamo = chamo + 1;
}

mientras (chamo mayol=ke 0) echale pichon {
    letra = 'a';
}

porsia (verdaita mayolke letra) {
    verdaita = chimbo;
}

